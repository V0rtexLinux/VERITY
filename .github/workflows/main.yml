name: Build ECHO

on:
  push:
    branches:
      - main
      - master
      - 'claude/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build with Windows and Java 25
    runs-on: windows-latest

    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: gradle

      - name: Show Java version
        run: |
          java -version
          javac -version

      - name: Build mod
        working-directory: echo
        run: .\gradlew.bat clean build --no-daemon --stacktrace

      - name: Check generated JAR
        working-directory: echo
        run: |
          $jars = Get-ChildItem "build/libs/*.jar" |
            Where-Object { $_.Name -notlike "*-sources.jar" }

          if ($jars.Count -eq 0) {
            throw "No mod JAR was produced in build/libs."
          }

          $jars | Format-Table Name, Length

      - name: Upload mod JAR
        uses: actions/upload-artifact@v4
        with:
          name: echo-mod-${{ github.sha }}
          path: |
            echo/build/libs/*.jar
            !echo/build/libs/*-sources.jar
          if-no-files-found: error
          retention-days: 30
