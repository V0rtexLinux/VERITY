- [MC 26.1.2 Fabric migration](mc26-fabric-migration.md) — verified API names
  (`ResourceKey.identifier()`, `Level.getDefaultClockTime()`, `PermissionSet`,
  `Item.Properties.setId`), registry gotchas, client `Options`, and how to
  type-check the mod offline against the cached Minecraft jar.
